# HTTP requests to career pages / hidden JSON APIs
requests

# Load email credentials from a local .env file during development
python-dotenv

# NOTE: everything else the tool uses is in Python's standard library and
# doesn't belong here — sqlite3 (storage), smtplib/email (mail), json, re,
# html, datetime, concurrent.futures.
